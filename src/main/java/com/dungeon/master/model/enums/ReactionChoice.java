package com.dungeon.master.model.enums;

/** A player's answer to a combat reaction prompt: cast a reaction spell or let the hit land. */
public enum ReactionChoice {
    SHIELD,
    ABSORB,
    DECLINE
}
