package com.dungeon.master.model.enums;

/** Category of an inventory item — drives behaviour (e.g. POTION_HEALING heals). */
public enum ItemKind {
    POTION_HEALING,
    POTION,
    SCROLL,
    WEAPON,
    ARMOR,
    GEAR
}
