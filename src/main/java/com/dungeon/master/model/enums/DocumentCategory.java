package com.dungeon.master.model.enums;

public enum DocumentCategory {
    LORE,
    RULES,
    SESSION_HISTORY,
    /** A prior session's end-of-session recap, seeded into a continuing session for retrieval. */
    CAMPAIGN_RECAP
}
