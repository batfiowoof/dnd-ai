package com.dungeon.master.model.enums;

/** Broad category of a quest, used to group and label quests in the World Builder and game log. */
public enum QuestType {
    MAIN,
    SIDE,
    PERSONAL
}
